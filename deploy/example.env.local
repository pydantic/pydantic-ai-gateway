LOGFIRE_ENVIRONMENT=dev

# generate a write token at https://logfire.pydantic.dev/
LOGFIRE_TOKEN=pylf_v1_...

# if you want to use OpenAI, generate an API key (you would use env.OPENAI_API_KEY in config.ts)
OPENAI_API_KEY=...

# same for Groq (again you would use env.GROQ_API_KEY in config.ts)
GROQ_API_KEY=...

# if you want to use Google Vertex, generate a service account key, copy the contents of the JSON file with no newlines
# you might want to use this one liner:
# python -c "import json;print(json.dumps(json.loads(open(input('Service account JSON file path: ')).read())))"
GOOGLE_SERVICE_ACCOUNT_KEY=full service google service account key...
